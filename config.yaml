
server:
  ip: 0.0.0.0
  port: 8080

database:
  dialect: "postgres"
  port: 54320
  dbuser: "postgres"
  dbpassword: "postgres"
  dbname: "ia_text"
  sslmode: "disable"
  dbcp:
    maxidle: 20
    maxactive: 200

cache:
  network: "tcp"
  ip: "localhost"
  port: 63790
  dbcp:
    maxidle: 80
    maxactive: 12000

InitDatabase: false